# Supabase

Ce dossier est reserve a la configuration Supabase et aux migrations SQL du projet Jarvis.

Premiere etape recommandee:

1. declarer les tables `organizations`, `users`, `prospects`, `calls`, `actions` et `daily_kpis`
2. ajouter les politiques RLS avant toute integration front
3. stocker les secrets d'integration uniquement cote API
